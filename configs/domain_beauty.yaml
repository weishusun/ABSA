domain: beauty
content_field: review
url_field: url
platform_field: platform
ctime_field: created_at
like_field: likes
reply_field: replies
keep_emoji: true
keep_english: false
min_length: 3
noise_suffix_patterns:
  - "(以上内容仅代表).*"
extra_noise_patterns:
  - "(买家秀).*"
extra_fields:
  - shade
splitter:
  min_len: 2
  max_len: 120
write_csv: false
